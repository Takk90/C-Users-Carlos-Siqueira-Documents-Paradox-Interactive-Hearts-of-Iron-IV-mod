#########################
#	AI_HACK_EVENTS		#
#########################

add_namespace = ai_taxes_war

#Mananges all the hack events for the AI so they can at least function
country_event = {
	id = ai_taxes_war.1
	
	hidden = yes
	is_triggered_only = yes
	
	option = {
		subtract_from_variable = { tax_rate = 10 }
		clamp_variable = { var = tax_rate min = 0 max = 100 }
		decrease_tax_cost = yes
		calculate_tax_gain = yes
		update_military_rate = yes
		ingame_calculate_size_modifier = yes
		calculate_resource_sale_rate = yes
		log = "[GetDateText]: [This.GetName]: is updating taxes through hack"
	}
}

country_event = {
	id = ai_taxes_war.2
	
	hidden = yes
	is_triggered_only = yes
	
	option = {
		add_to_variable = { tax_rate = 10 }
		clamp_variable = { var = tax_rate min = 0 max = 100 }
		decrease_tax_cost = yes
		calculate_tax_gain = yes
		update_military_rate = yes
		ingame_calculate_size_modifier = yes
		calculate_resource_sale_rate = yes
		log = "[GetDateText]: [This.GetName]: is updating taxes through hack"
	}
}