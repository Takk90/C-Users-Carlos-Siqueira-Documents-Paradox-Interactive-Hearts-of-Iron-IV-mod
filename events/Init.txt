add_namespace = init

#init.0
#Fires at the start of the game. So any events that need to be trigggered at the start put them here
#and also in init.2 if a newly formed country needs to get that event

country_event = {
	id = init.0
	title = init.0.t
	desc = init.0.d
	
	#major = yes
	hidden = yes
	#fire_only_once = yes
	is_triggered_only = yes
	
	immediate  = {
		
		#every_country = {
			country_event = {id = init.1} #Initial Pop up at the start of the game
			country_event = {id = crime_fighting.2} #Sets Crime Fighting Flags
			country_event = {id = economic_cycle.1} #Sets economic cycle flags
			country_event = {id = gdp.1 } #Sets gdp/c clags
			country_event = {id = brotherhood.0 } #Sets muslim_brotherhood relations
			country_event = {id = brotherhood.2 }
			set_country_flag = Start_events_initialized
			country_event = { id = debt_count.1 days = 75 }
			set_country_flag = {
			flag = debt_counting
			value = 25
		    }
			country_event = { id = state_update.2 } #states debuff 
			country_event = { id = set_spirit.1 } #sets multi-ethnic spirit
		#}
		
	}
	option = {
		name = init.0.a
		#this = {gearz_test = yes}
	}
	
	
}

#Creates the pop up at the start of the game that explains things
country_event = {
	id = init.1
	title = init.1.t
	
	desc = {
		text = init.1.d
		trigger = { not = { tag = SYR tag = FSA tag = NUS tag = ROJ tag = TUR tag = KUR tag = IRQ has_idea = GCC_member tag = PER } }
	}
	desc = { text = init.1.d_Syria trigger = { tag = SYR } }
	desc = { text = init.1.d_FSA trigger = { tag = FSA } }
	desc = { text = init.1.d_AL_NUSRA trigger = { tag = NUS } }
	desc = { text = init.1.d_ROJ trigger = { tag = ROJ } }
	desc = { text = init.1.d_TUR trigger = { tag = TUR } }
	desc = { text = init.1.d_KUR trigger = { tag = KUR } }
	desc = { text = init.1.d_IRQ trigger = { tag = IRQ } }
	desc = { text = init.1.d_GCC trigger = { has_idea = GCC_member } }
	desc = { text = init.1.d_PER trigger = { tag = PER } }
	
	is_triggered_only = yes
	
	
	immediate  = {
		hidden_effect = {
			#Executes PP & NU system just incase they don't click both option A & B
			set_pp_gain = yes
			set_NU_gain = yes	
		}
	}
	
	# option = {
		# name = init.1.a
		
		# trigger = {
			# this = { is_ai = no }
		# }
		
		# country_event = {id = political_power.2}
	# }
	
	# option = {
		# name = init.1.b
		
		# trigger = {
			# this = { is_ai = no }
		# }
		
		# country_event = {id = national_unity.1}
	# }
	
	# option = {
		# name = init.1.c
	# }
	option = {
		name = init.1.e
	}
	
	
}

#This is only fired for new countries. There is no reason to trigger this for every country
country_event = {
	id = init.2
	title = init.2.t
	desc = init.2.d
	
	hidden = yes
	#fire_only_once = yes
	
	is_triggered_only = yes
	
	immediate  = {
		set_pp_gain = yes
		set_NU_gain = yes	
		set_crime_fighting_flags = yes
		set_economic_cycle_flags = yes
		set_gdpc_flags = yes
		country_event = { id = debt_count.1 days = 75 }
		set_country_flag = {
			flag = debt_counting
			value = 25
	   }
	}
	option = {
		name = init.2.a
		#this = {gearz_test = yes}
	}
	
	
}