add_namespace = init

#Fires at the start of the game by

country_event = {
	id = init.0
	title = init.0.t
	desc = init.0.d
	
	major = yes
	hidden = yes
	#fire_only_once = yes
	
	trigger = {
		#tag = GER
		date > 1936.1.1
		not = {has_country_flag = pp_gain_set}
	}
	
	
	mean_time_to_happen = {
        days = 2
    }
	
	
	immediate  = {
		country_event = {id = init.1}
	}
	option = {
		name = init.0.a
		#this = {gearz_test = yes}
	}
	
	
}

country_event = {
	id = init.1
	title = init.1.t
	desc = init.1.d
	
	is_triggered_only = yes
	
	
	immediate  = {
		hidden_effect = {
			#Executes PP & NU system just incase they don't click both option A & B
			set_pp_gain = yes
			set_NU_gain = yes	
		}
	}
	
	option = {
		name = init.1.a
		
		limit = {
			this = { is_ai = no}
		}
		
		country_event = {id = political_power.2}
	}
	
	option = {
		name = init.1.b
		
		limit = {
			this = { is_ai = no}
		}
		
		country_event = {id = national_unity.1}
	}
	
	option = {
		name = init.1.c
	}
	option = {
		name = init.1.e
	}
	
	
}