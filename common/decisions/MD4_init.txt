MD4_init = {
	MD4_init_stability = {
		
		# allowed = {
			# always = yes
		# }
		
		available = {
			has_country_flag = MD4_init_stability
			is_ai = yes
		}

		visible = {
			this = { has_country_flag = MD4_init_stability }
		}
		# target_trigger = {
			# has_country_flag = MD4_init_stability
		# }

		Is_good = yes

		selectable_mission = yes
		days_mission_timeout = 1
		days_remove = 30
		
		
		
		timeout_effect = {
			if = {
				limit = { has_country_flag = MD4_init_stability }
				clr_country_flag = MD4_init_stability
				random_country = {
					limit = { is_ai = yes has_war = no }
					set_country_flag = MD4_init_stability
					country_event = { id = low_stability.1 }
					#log
				}
			}
			#log = "[GetDateText]: [This.GetName]: MD4_init_stability decision timed out"
		}


		complete_effect = {
			#log = "[GetDateText]: [This.GetName]: MD4_init_stability decision executed"
			every_country = {
				country_event = { id = low_stability.1 }
			}
			#log = "[GetDateText]: [This.GetName]: MD4_init_debt decision finished execution"
		}
		
		ai_will_do = {
			factor = 100
		}
	}
	MD4_init_debt = {
		
		# allowed = {
			# always = yes
		# }
		
		available = {
			has_country_flag = MD4_init_debt
			is_ai = yes
		}

		visible = {
			this = { has_country_flag = MD4_init_debt }
		}
		# target_trigger = {
			# has_country_flag = MD4_init_debt
		# }

		Is_good = yes

		selectable_mission = yes
		days_mission_timeout = 1
		days_remove = 75
		

		
		timeout_effect = {
			if = {
				limit = { has_country_flag = MD4_init_debt }
				clr_country_flag = MD4_init_debt
				random_country = {
					limit = { is_ai = yes has_war = no }
					set_country_flag = MD4_init_debt
					country_event = { id = debt_count.1 }
				}
			}
			#log = "[GetDateText]: [This.GetName]: MD4_init_debt decision timed out"
		}


		complete_effect = {
			#log = "[GetDateText]: [This.GetName]: MD4_init_debt decision executed"
			every_country = {
				country_event = { id = debt_count.1 }
			}
			#log = "[GetDateText]: [This.GetName]: MD4_init_debt finished execution"
		}
		
		ai_will_do = {
			factor = 100
		}
	}
	
}