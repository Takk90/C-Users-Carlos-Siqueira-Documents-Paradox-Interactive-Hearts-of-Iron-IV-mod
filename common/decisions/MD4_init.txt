MD4_init = {
	MD4_init = {
		
		#allowed = { FROM = { not = { tag = ROOT } } }
		
		available = {
			has_country_flag = MD4_init
			is_ai = yes
			#tag = GER
		}

		visible = {
			is_ai = yes
			has_country_flag = MD4_init
		}

		Is_good = yes
		
		#cancel_if_not_visible = yes
		selectable_mission = yes
		days_mission_timeout = 5
		days_remove = 25
		
		timeout_effect = {
			if = {
				limit = { has_country_flag = MD4_init }
				clr_country_flag = MD4_init
				random_country = {
					limit = { is_ai = yes has_war = no }
					set_country_flag = MD4_init
					PREV = { clr_country_flag = MD4_init }
					# every_country = {
						# limit = { or = { is_target_of_coup = yes has_civil_war = yes has_stability < 0.36 } }
						# country_event = low_stability.1
					# }
				}
				
			}
			#log = "[GetDateText]: [This.GetName]: MD4_init_stability decision timed out"
		}
		
		# remove_effect = {
			# random_country = {
					# limit = { is_ai = yes has_war = no }
					# set_country_flag = MD4_init
					# PREV = { clr_country_flag = MD4_init }
					# every_country = {
						# limit = { or = { is_target_of_coup = yes has_civil_war = yes has_stability < 0.36 } }
						# country_event = low_stability.1
					# }
				# }
		# }


		complete_effect = {
			#log = "[GetDateText]: [This.GetName]: MD4_init_stability decision executed"
			every_country = {
				limit = { or = { is_target_of_coup = yes has_civil_war = yes has_stability < 0.36 } }
				country_event = low_stability.1
			}
			#log = "[GetDateText]: [This.GetName]: MD4_init_debt decision finished execution"
		}
		
		ai_will_do = {
			factor = 1000
		}
	}
	
}