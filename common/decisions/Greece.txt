GRE_communist_economy_decision_category = {
    
	GRE_decision_industrilazation = {
        icon = political_actions 
        cost = 100

        available = { 
            has_completed_focus = GRE_kke
            NOT = { has_country_flag = kke_flag }
        }
        days_remove = 100
        fire_only_once = yes
        days_re_enable = 0
        complete_effect = { set_country_flag = kke_flag }
        modifier = {
            line_change_production_efficiency_factor = 0.05
            industrial_capacity_factory = 0.05
        }
        remove_effect = { clr_country_flag = kke_flag }
}
GRE_decision_indoctrination = {
        icon = political_actions 
        cost = 100

        available = { 
            has_completed_focus = GRE_kke
            NOT = { has_country_flag = kke_flag }
        }
        days_remove = 100
        fire_only_once = yes
        days_re_enable = 0
        complete_effect = { set_country_flag = kke_flag }
        modifier = {
             communism_drift = 0.10
			 political_power_gain = 0.05
			 stability_weekly = -0.05
        }
        remove_effect = { clr_country_flag = kke_flag }
}
GRE_decision_crackdown = {
        icon = political_actions 
        cost = 100

        available = { 
            has_completed_focus = GRE_kke
            NOT = { has_country_flag = kke_flag }
        }
        days_remove = 100
        fire_only_once = yes
        days_re_enable = 0
        complete_effect = { set_country_flag = kke_flag }
        modifier = {
             democratic_drift = -0.10
			 nationalist_drift = -0.10
			 political_power_gain = 0.10
			 stability_weekly = -0.05
        }
        remove_effect = { clr_country_flag = kke_flag }
}
GRE_decision_monuments  = {
        icon = political_actions 
        cost = 100

        available = { 
            has_completed_focus = GRE_kke
            NOT = { has_country_flag = kke_flag }
        }
        days_remove = 100
        fire_only_once = yes
        days_re_enable = 0
        complete_effect = { set_country_flag = kke_flag }
        modifier = {
             democratic_drift = -0.10
			 nationalist_drift = -0.10
			 political_power_gain = 0.10
			 stability_weekly = -0.05
        }
        remove_effect = { clr_country_flag = kke_flag }
}
GRE_decision_army = {
        icon = political_actions 
        cost = 100

        available = { 
            has_completed_focus = GRE_kke
            NOT = { has_country_flag = kke_flag }
        }
        days_remove = 100
        fire_only_once = yes
        days_re_enable = 0
        complete_effect = { set_country_flag = kke_flag }
        modifier = {
             army_defence_factor = 0.03
			 experience_gain_army = 0.025
        }
        remove_effect = { clr_country_flag = kke_flag }
}
GRE_decision_nationalization = {
		icon = political_actions 
		icon = political_actions 
        cost = 100

        available = { 
            has_completed_focus = GRE_kke
            NOT = { has_country_flag = kke_flag }
        }
        days_remove = 100
        fire_only_once = yes
        days_re_enable = 0
        complete_effect = { set_country_flag = kke_flag }
        modifier = {
             consumer_goods_factor = -0.02
        }
        remove_effect = { clr_country_flag = kke_flag }
     }
	
}






