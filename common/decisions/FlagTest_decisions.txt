#Category only allowed for CHI
ENG_CHANGE_FLAGS = {
	eng_communist_flag = {
		allowed = { tag = ENG }


		available = {
			or = {
				ENG = { 
				has_government = communism
				hidden_effect = {
				NOT = {
				has_cosmetic_tag = ENG_communism_flag
				has_country_flag = communist_flag
				}
				}
				}
				
			}
			
		}


		activation = {
			ENG = {
				has_government = communism
				NOT = {
				has_cosmetic_tag = ENG_communism_flag
				has_country_flag = communist_flag
				}
			}
		}


		Is_good = yes

		days_re_enable = 365
		days_remove = 1
		


		complete_effect = {
			add_political_power = 25
			hidden_effect = {
			set_cosmetic_tag = ENG_communism_flag
			set_country_flag = communist_flag
			}
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}