#Scripted triggers				Scope				Explanation													
#can_upgrade_gdp				COUNTRY SCOPE		TRUE if SCOPE COUNTRY GDP can be upgraded


#Checks if a country is classified as a western nation
can_upgrade_gdp = {
	OR = {
		and = {
			has_idea = generic_gdp_1
			OR = {
				AND = {
					num_of_factories < 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 0
							   include_locked = no
							}
						}
					} 	
				}
				AND = {
					num_of_factories > 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 1
							   include_locked = no
							}
						}
					} 	
				}
			}
		}
		and = {
			has_idea = generic_gdp_2
			OR = {
				AND = {
					num_of_factories < 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 0
							   include_locked = no
							}
						}
					} 	
				}
				AND = {
					num_of_factories > 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 1
							   include_locked = no
							}
						}
					} 	
				}
			}
		}
		and = {
			has_idea = generic_gdp_3
			OR = {
				AND = {
					num_of_factories < 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 0
							   include_locked = no
							}
						}
					} 	
				}
				AND = {
					num_of_factories > 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 1
							   include_locked = no
							}
						}
					} 	
				}
			}
		}
		and = {
			has_idea = generic_gdp_4
			OR = {	
				AND = {
					num_of_factories < 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 0
							   include_locked = no
							}
						}
					} 	
				}
				AND = {
					num_of_factories > 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 1
							   include_locked = no
							}
						}
					} 	
				}
			}
		}
		and = {
			has_idea = generic_gdp_5
			OR = {
				AND = {
					num_of_factories < 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 0
							   include_locked = no
							}
						}
					} 	
				}
				AND = {
					num_of_factories > 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 1
							   include_locked = no
							}
						}
					} 	
				}
			}
		}
		and = {
			has_idea = generic_gdp_6
			OR = {
				AND = {
					num_of_factories < 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 0
							   include_locked = no
							}
						}
					} 	
				}
				AND = {
					num_of_factories > 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 1
							   include_locked = no
							}
						}
					} 	
				}
			}
		}
		and = {
			has_idea = generic_gdp_7
			OR = {
				AND = {
					num_of_factories < 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 0
							   include_locked = no
							}
						}
					} 	
				}
				AND = {
					num_of_factories > 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 1
							   include_locked = no
							}
						}
					} 	
				}
			}
		}
		and = {
			has_idea = generic_gdp_8
			OR = {
				AND = {
					num_of_factories < 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 0
							   include_locked = no
							}
						}
					} 	
				}
				AND = {
					num_of_factories > 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 1
							   include_locked = no
							}
						}
					} 	
				}
			}
		}
		and = {
			has_idea = generic_gdp_9
			OR = {
				AND = {
					num_of_factories < 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 0
							   include_locked = no
							}
						}
					} 	
				}
				AND = {
					num_of_factories > 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 1
							   include_locked = no
							}
						}
					} 	
				}
			}
		}
		and = {
			has_idea = generic_gdp_10
			OR = {
				AND = {
					num_of_factories < 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 0
							   include_locked = no
							}
						}
					} 	
				}
				AND = {
					num_of_factories > 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 1
							   include_locked = no
							}
						}
					} 	
				}
			}
		}
		and = {
			has_idea = generic_gdp_11
			OR = {
				AND = {
					num_of_factories < 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 0
							   include_locked = no
							}
						}
					} 	
				}
				AND = {
					num_of_factories > 79
					NOT = {
						any_owned_state = {
							free_building_slots = {
							   building = industrial_complex
							   size > 0
							   include_locked = no
							}
						}
					} 	
				}
			}
		}
		
	}
}