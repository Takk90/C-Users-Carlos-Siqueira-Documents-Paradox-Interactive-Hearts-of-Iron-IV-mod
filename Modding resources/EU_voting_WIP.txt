# UN Vote mechanism
add_namespace = EU

country_event = {
	id = EU.1
	title = EU.1.t
	desc = EU.1.d
	
	is_triggered_only = yes

    immediate = { }
	
	option = {
		name = EU.1.a
		set_global_flag = { flag = EU_majority_vote value = 1000 }
		every_country = { 
				limit = { 
					has_idea = EU_member
					}
			hidden_effect = { country_event = { id = EU.2 hours = 6 } }		
		}
		hidden_effect = { news_event = { id = EU.3 days = 3 } }
	}
}