kurd_activism = {

    #Stop the rallies
	stop_rallies = {
		icon = generic_political_discourse
		
		fire_only_once = yes
		cost = 50
		
		allowed = {
		original_tag = TUR 
		}
		
		visible = {
		has_country_flag = kurd_rally1
		has_country_flag = kurd_activism
		}
		
		available = {
		TUR = {
		    controls_state = 162
			has_idea = multi_ethnic_state_idea
		    }
		PKK = {
	        exists = no	
		    }
		}
		
		ai_will_do = {
		factor = 1 
		}

		complete_effect = {
		add_national_unity = -0.10
		clr_country_flag = kurd_rally1
		hidden_effect = {
	    country_event = { id = kurd_rally.3 days = 1 } 
	        }
		}
	}
	
	#Increase Security Level
	security_increase = {
		icon = generic_political_discourse
		
		fire_only_once = yes
		cost = 50
		
		allowed = {
		original_tag = TUR 
		}
		
		visible = {
		has_country_flag = kurd_activism
		}
		
		available = {
		TUR = {
		    controls_state = 162
			has_idea = multi_ethnic_state_idea
		    }
		PKK = {
	        exists = no	
		    }
		}
		
		ai_will_do = {
		factor = 1 
		}

		complete_effect = {
		set_country_flag = tur_security_increase
		}
	}
	
	#Open up talks with the Kurds
	kurd_talks = {
		icon = generic_political_discourse
		
		fire_only_once = yes
		cost = 50
		
		allowed = {
		original_tag = TUR 
		}
		
		visible = {
		has_country_flag = kurd_activism
		}
		
		available = {
		TUR = {
		    controls_state = 162
			has_idea = multi_ethnic_state_idea
		    }
		PKK = {
	        exists = no	
		    }
		}
		
		ai_will_do = {
		factor = 1 
		}

		complete_effect = {
		add_national_unity = -0.10
		clr_country_flag = kurd_rally1
		}
	}

}